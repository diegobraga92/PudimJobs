celery>=5.4.0
kombu>=5.4.0
redis>=5.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0
prometheus-client>=0.20.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pydantic-settings>=2.0.0
structlog>=24.0.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
# Instrumentation packages ship only as pre-releases (e.g. 0.65b0); pin exact
# versions so `pip install` without --pre can resolve them.
opentelemetry-instrumentation-celery==0.65b0
opentelemetry-exporter-otlp>=1.24.0

# The worker shares models/config with the backend (backend/app is mounted or
# copied into the image at /app/backend).
